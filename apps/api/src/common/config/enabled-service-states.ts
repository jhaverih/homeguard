// States vendors can currently select counties in. Expanding coverage to a
// new state is just adding its 2-letter abbreviation here — the underlying
// county data (us-counties.json/zip-county.json) already covers all of them.
export const ENABLED_SERVICE_STATES: string[] = ['TN'];
